import Button from "../../../common/components/UlElements/AnimatedButton";
import React from "react";

const Hero = () => {
  return (
    <div>
      <Button primary>按鈕</Button>
    </div>
  );
};

export default Hero;
